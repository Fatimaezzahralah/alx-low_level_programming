#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: int to be checked
 *
 * Return: if c is a digit 1, otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
